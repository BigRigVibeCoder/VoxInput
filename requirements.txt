--extra-index-url https://download.pytorch.org/whl/cu118

# ── Core ─────────────────────────────────────────────────────────────────────
vosk>=0.3.45
pyaudio>=0.2.14
pynput>=1.7.7
numpy

# ── Torch (CUDA build — drops to CPU if GPU unavailable) ─────────────────────
torch==2.7.1+cu118
torchvision==0.22.1+cu118
torchaudio==2.7.1+cu118

# ── Speech engines ────────────────────────────────────────────────────────────
openai-whisper

# Optional — significant accuracy/speed improvement (Phase 2)
# faster-whisper>=1.0.0

# ── Phase 3: Spell correction ─────────────────────────────────────────────────
# Optional — falls back to pass-through if not installed
# symspellpy>=6.7.0

# ── Phase 5: UI (system GTK3 + Cairo is used; pycairo via apt preferred) ─────
# pycairo>=1.20.0   # already provided by python3-gi-cairo (apt)

# ── System deps (installed by install.sh, listed here for reference) ──────────
# python3-gi python3-gi-cairo gir1.2-gtk-3.0
# libportaudio2 portaudio19-dev
# xdotool ydotool (Phase 4 Wayland backend)
# ffmpeg (Whisper audio decoding)
